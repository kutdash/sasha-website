---
---

<section id="approach" class="approach section">
  <div class="container">
    <p class="h-eyebrow reveal">мой подход</p>
    <h2 class="h-1 reveal">
      обучение строится <span class="approach__accent">под твои задачи</span>
    </h2>

    <p class="approach__lead reveal">
      продвижение и прокачка видеографии — два направления роста. ты сама выбираешь акцент: фокус на одном или работа в обоих сразу
    </p>
  </div>
</section>

<style>
  .approach .h-1 {
    margin-top: 12px;
    margin-bottom: 32px;
    max-width: 18ch;
  }
  .approach__accent {
    color: var(--pink-deep);
  }
  .approach__lead {
    font-size: clamp(18px, 1.6vw, 24px);
    line-height: 1.5;
    color: var(--ink-soft);
    max-width: 56ch;
  }
</style>
